import React, { FC } from 'react'
import "./Modal.css"

type ModalProps = {
    title: string;
    body: string;
}


export const Modal: FC<ModalProps> = () => {
    return (
        <div>

        </div>
    )
}
